# keyboard-configs

## Laptop

```bash
sudo tmux kill-session -t kanata
sudo tmux ls
sudo tmux new -d -s kanata '/opt/homebrew/bin/kanata --cfg /Users/tobyc/Documents/repos_personal/keyboard-configs/glove80.kbd'
```

## Personal Laptop

```bash
sudo tmux new -d -s kanata '/opt/homebrew/bin/kanata --cfg /Users/tcourtis/repos/keyboard-configs/glove80.kbd'
```
